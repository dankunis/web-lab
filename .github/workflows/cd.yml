name: CD

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Deploy once tha master branch has been pushed to
      run:  |
        echo Building project...
        echo Connecting to server...
        echo Deploying...
        echo Done!



jobs:
  deploy:
    runs-on: ubuntu-latest
if: github.event.pull_request.merged

    steps:
      - name: checkout
        if: github.event.pull_request.merged
        uses: actions/checkout@v1
with:
ref: master
